export enum PLATFORMS {
  IOS = "ios",
  ANDROID = "android",
  WINDOWS = "windows",
  MACOS = "macos",
  WEB = "web",
}

export enum PAGES {
  HOME = "home",
  SEARCH = "search",
  LIBRARY = "library",
}
